name: Close → Google Sheets Sync

on:
  schedule:
    - cron: "0 0 */3 * *"   # Runs every 3 days — change if needed
  workflow_dispatch:      # Allows manual run

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          pip install requests gspread google-auth google-auth-oauthlib google-auth-httplib2 google-api-python-client

      - name: Create Google Service Account file
        env:
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
        run: |
          echo "$GOOGLE_SERVICE_ACCOUNT_JSON" > service-account.json

      - name: Run sync script
        env:
          CLOSE_API_KEY: ${{ secrets.CLOSE_API_KEY }}
          SHEET_ID: ${{ secrets.SHEET_ID }}
        run: |
          python sync.py
